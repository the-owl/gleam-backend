database:
  entities:
    - "dist/entity/**/*.js"
  migrations:
    - "dist/migration/**/*.js"
  type: postgres
  host: localhost
  username: gleam
  password: ''
  database: gleam
http:
  port: 8000
